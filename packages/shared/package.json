{
  "name": "@my-app/shared",
  "version": "0.0.1",
  "description": "Shared library",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^16.11.9",
    "typescript": "^4.5.2"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf ./dist",
    "build:clean": "yarn clean && yarn build"
  }
}
